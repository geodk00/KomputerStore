# JS Task 1 - Komputer store
This is an implementation of the "store" described in task 1.

The functionality is implemented in `script.js`.

The CSS was written without the use of a css framework (and it shows). 
